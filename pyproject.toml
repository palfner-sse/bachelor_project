[project]
name = "bachelor-project"
version = "0.1.0"
requires-python = ">=3.12.13"
dependencies = [
    "besser>=7.0.0",
    "claude-agent-sdk>=0.2.102",
    "langchain>=1.3.9",
    "langgraph>=1.2.5",
]

[tool.setuptools.packages.find]
where = ["src"]
